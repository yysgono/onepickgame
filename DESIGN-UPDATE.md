# One Pick Game — 흰색 테마 업데이트 v4

## 이번 수정 (v4)

- 홈의 이상형 월드컵 추천 제목을 가운데 정렬했습니다.
- Language 라벨에는 기본 커서를 적용하고 실제 언어 선택 드롭다운에만 클릭 커서를 유지했습니다.
- 홈 카드 버튼 글씨를 PC 19px, 모바일 17px로 확대했습니다.
- 사용자 티어표 카드는 PC에서 최소 420px 열 너비를 사용하며 작은 화면에서는 2열/1열로 전환됩니다. 추천 티어표 영역도 최대 1440px로 확대했습니다.
- 월드컵/티어표 검색 입력창 높이를 PC 60px, 모바일 54px로 확대했습니다.
- 티어표 카테고리와 정렬 필터를 추천 카드 아래, 사용자 티어표 카드 목록 위로 실제 이동했습니다.
- ResultPage에 남아 있던 onepick.png 배경과 검은 덮개를 제거했습니다.
- 게임 후보와 패자부활전 후보의 기본 테두리를 진하게 하고 선택 상태를 보라색으로 강조했습니다.

## 이전 수정 (v3)

- 카드 설명을 내부 텍스트 요소로 분리했습니다. 내부 패딩 없이 두 줄 높이와 말줄임을 적용해 세 번째 줄이 노출되는 문제를 수정했습니다.
- 홈 카드 가로 폭을 PC 420px, 모바일은 화면에 맞춰 최대 360px로 늘렸습니다.
- 월드컵과 티어표에 공통 PageIntro 컴포넌트를 적용해 제목/설명/만들기 버튼의 크기와 간격을 통일했습니다.
- 헤더의 언어 선택을 Language 라벨과 함께 크게 표시합니다. 모바일에서는 헤더 안 별도 전체 너비 행에 항상 표시합니다.
- 원본 로고 이미지 파일은 유지하고 이미지 전체를 담도록 표시 방식을 바꿨습니다. 자르기, 필터, 변형을 제거했습니다. 원본 자체의 화질을 인공지능으로 재생성하거나 향상한 것은 아닙니다.

## 이전 수정 (v2)

- 헤더 로고에 고정 비율 원형 틀을 추가하고 이미지의 너비/높이를 명시해 찌그러지거나 원이 잘리는 문제를 수정했습니다. 원본 이미지 파일은 유지했습니다.
- 헤더와 월드컵·티어표 목록의 최대 너비 제한을 제거해 좌우 공간을 사용하도록 했습니다.
- 전체 컴포넌트의 글자 크기를 기본 2px씩 키우고 카드 높이·제목 영역도 함께 조정했습니다.
- 월드컵과 티어표 카드 제목을 가운데 정렬했습니다.
- 월드컵 카드 하단에 카테고리별 5px 색상선을 추가했습니다. 티어표 카드에는 파란색 하단선을 적용했습니다.
- 카테고리 사이에 가로 구분선을 추가했습니다.
- 라운드 선택 상세 화면의 후보·이용 안내·게임 정보에 테두리와 제목 구분선을 추가하고 그림자를 줄였습니다.

## 적용 방법

1. 기존 프로젝트를 백업하세요.
2. ZIP 안 `onepickgame` 폴더의 내용을 기존 프로젝트 루트에 덮어쓰세요. `src`, `public`, 설정 파일을 함께 적용해야 합니다.
3. 기존 `.env` 파일과 서버의 환경변수는 그대로 사용하세요. 이 ZIP에는 환경변수 파일, Git 이력, 설치된 의존성, 과거 데이터 백업을 포함하지 않았습니다.
4. 의존성이 없는 새 폴더에서 실행한다면 `npm ci`, 기존 폴더라면 `npm start`로 확인하세요.
5. 배포 시 기존 `npm run build`를 사용하세요. 기존 sitemap 생성 단계는 Supabase 연결이 필요합니다.

## 디자인 변경

- 전체 기본 배경을 흰색으로 변경하고 고정 배경 이미지와 격자 배경을 제거했습니다.
- 검은색·짙은 남색 패널을 흰색/옅은 회색으로 정리했습니다. 제목은 짙은 회색, 보조 정보는 읽기 쉬운 회색/보라색으로 조정했습니다.
- 원본 로고 `public/onepick2.png`를 변경하지 않고 사용합니다.
- 헤더의 현재 게임 메뉴 칸 전체에 보라색 배경과 흰색 글자를 적용했습니다. 준비 중인 메뉴는 별도로 표시합니다.
- 헤더에서 만들기 버튼을 제거하고 월드컵·티어표 페이지 상단 중앙에 큰 만들기 버튼을 배치했습니다.
- 헤더 오른쪽에 `내 콘텐츠`를 추가했습니다. 기존 `my-worldcups`와 `tier-list?mine=1` 경로를 탭으로 연결했습니다.
- 프로필 수정, 로그아웃, 관리자 기능은 프로필 메뉴에 모았습니다. 언어 선택과 Blog 이동을 유지했습니다.
- 월드컵 카드 너비·글자 크기·여백을 조정하고 네온 효과를 줄였습니다.
- 회원가입/로그인, 제작/수정, 플레이, 결과/통계, 댓글, 관리 화면의 색상을 함께 정리했습니다.
- 입력창/자리표시자, 선택된 필터, 마우스 오버, 모달, 주요 버튼의 글자 대비를 조정했습니다.
- 화면 크기에 따라 헤더가 두 줄로 정리되고 모바일에서도 내 콘텐츠와 프로필에 접근할 수 있도록 반응형 규칙을 추가했습니다.
- 내 월드컵 목록은 실제 로그인 사용자 ID/이메일에 해당하는 항목만 표시하도록 정리했습니다. 비로그인 상태에서는 로그인 안내를 제공합니다.

## 주요 수정 파일

- `src/components/Header.js`, `src/components/OnePickHeader.css`: 새 헤더, 활성 메뉴, 계정 메뉴.
- `src/components/ContentNav.js`: 내 콘텐츠 탭과 로그인 안내.
- `src/styles/light-theme.css`: 공통 색상, 타이포그래피, 카드, 버튼, 반응형 규칙.
- `src/App.js`, `src/index.js`, `public/index.html`: 테마 연결, 배경 처리, 내 콘텐츠 연결.
- `src/components/Home.js`, `TierListPage.js`: 목록/만들기/필터 디자인.
- 나머지 컴포넌트와 공통 스타일: 페이지 안에 직접 지정된 배경색·글자색·그림자 조정.

## 검증 범위와 한계

- React 프로덕션 프런트엔드 빌드: 통과. 실행 명령은 `node node_modules/react-scripts/bin/react-scripts.js build`입니다. 외부 연결이 필요한 sitemap 생성 단계는 실행하지 않았습니다.
- 샘플 응답을 사용한 DOM 검사: 홈, 티어표, 내 콘텐츠, 제작, 로그인/회원가입, 라운드 선택, 통계, 카테고리, 티어표 결과, 블로그, 개인정보 페이지를 점검했습니다.
- DOM 검사에서는 데스크톱/모바일 너비 설정, 활성 메뉴, 원본 로고 경로, 만들기 버튼, 로그인 상태의 내 콘텐츠·프로필 모달을 확인했습니다.
- 표시된 텍스트의 계산 가능한 단색 배경 대비와 소스의 명시적 색상 조합을 검사하고 발견된 문제를 수정했습니다. 이는 접근성 전체 인증을 뜻하지 않습니다.
- 브라우저 바이너리 설치가 네트워크 제한으로 실패해 실제 픽셀 화면과 반응형 배치를 스크린샷으로 확인하지 못했습니다. 이미지·영상 위 텍스트, 긴 다국어 문구, 사용자 정의 티어 색상은 실제 브라우저에서 추가 확인이 필요합니다.
- 실제 서비스 계정으로 로그인하거나 콘텐츠를 저장·삭제·게시하지 않았습니다. 운영 데이터 및 배포 설정을 변경하지 않았습니다.

## 색상 변경 위치

공통 색상은 `src/styles/light-theme.css`의 `:root`와 헤더 CSS에서 관리합니다. 기존 코드에는 인라인 스타일도 남아 있으므로 향후 다른 테마를 추가할 때 해당 페이지 스타일도 함께 확인하세요.
